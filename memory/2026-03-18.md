# 2026-03-18

- Heartbeat memory maintenance run after >3 hours since the last memory update.
- Recent durable Telegram troubleshooting context: the Telegram bot/provider was healthy and online after restart, but reply generation appeared to stall in backend/model processing rather than in Telegram delivery itself.
- Evidence pattern from logs: Telegram provider started normally, chat showed typing activity, then hit typing TTL without a final response; user also saw repeated `API rate limit reached. Please try again later.` messages.
- Practical conclusion to retain: when Telegram appears frozen, first suspect backend/model congestion or rate limits before suspecting Telegram channel connectivity.
- XXTouch durable state from prior deployment work remains: `tiktok04:1-208` had `207/208` completed with machine `1` still problematic; `tiktok04:209-442` had `231/231` online machines updated, with `236`, `336`, `382` offline at that point.
- Overnight `Final.lua` run for `tiktok04:1-52` used 3 rounds in 10-machine batches. Outcome: round 1 `51/52` OK except machine `1`; round 2 `1/52` OK; round 3 `0/52` OK. Main repeated error after round 1 was `The system is currently running another script`, so user and assistant concluded current `Final.lua` is unreliable for farm use and should be considered discarded/reworked.
- User preference reinforced: assistant should do the technical work directly, keep updates short and practical, and report true progress/results.
- New durable workflow preference: when a fix is obvious from context, do the correction directly without asking for reconfirmation. The user explicitly wants fewer back-and-forth confirmation questions on iterative technical debugging.
- New UI/status preference: transient status/toast messages in XXTouch test scripts must be sparse, state-based, and readable long enough for the user to see them. Do not spam or rotate rapid status updates; only change status when the script changes state.
- Additional workflow rule from user: if a file edit/check reveals the assistant made a structural mistake, immediately continue to fix it in the same flow and state clearly that it is being fixed; do not stop at "I found the issue" and wait for the user to ask whether it has been fixed.
- New proxy-checker task context: user wants an Excel 365 proxy checker that reacts immediately when a proxy is pasted, continues background rechecks because proxies can die anytime, and treats duplicate detection only within the same sheet (not across sheets).
- Duplicate-display preference evolved during discussion: final rule is to print duplicate detections only in shell, not as special duplicate styling in Excel.
- Workspace artifacts created on PC side for the proxy-checker project under `D:\OpenClaw\Proxy_Check_Realtime`, including `check_proxy_realtime_v2.py` and then `check_proxy_sheet_only_v3.py` after requirement changes.
- Important unresolved issue for next session: user reports the latest proxy checker still behaves batch-first (checks existing rows/sheets in order) instead of giving true immediate priority to newly pasted cells. Next version should use a strict high-priority queue for new inputs and background recheck only when that priority queue is empty.
- XXTouch rewrite direction was reset: rebuild `Final.lua` from scratch in small validated stages. Stable boundaries were refined by the user: stable segment A is from script start through deleting TikTok and then waiting 60 seconds; stable segment B is the AppManager-open flow through its end. The unstable area is the bridge between those stable segments (after the delete/wait block and before/around the transition into AppManager). User wants the rewrite to proceed step-by-step, keeping already stable blocks aside and rebuilding only one unstable stage at a time.
- A new draft block combining stable popup handling with the new video-validity logic was accepted as temporarily stable enough to keep for reuse. Saved PC-side as `D:\OpenClaw\XXTouch_jobs\upload\Final_step_popup_video_ok.lua`.
- XXTouch `Final.lua` rewrite progressed with two isolated draft blocks on PC for staged testing. The popup+video stage was saved for reuse as `D:\OpenClaw\XXTouch_jobs\upload\Final_step_popup_video_ok.lua`; it includes the old stable popup block plus a new draft video-validity flow. A separate App Store-only test block was created as `D:\OpenClaw\XXTouch_jobs\upload\Final_step_appstore_download_test.lua` to handle only: open App Store -> open TikTok page -> tap cloud/download if needed -> wait until `OPEN`, without launching TikTok.
- The first popup+video draft initially failed because it used `touch.swipe`, which is not available in the XXTouch environment on the user's devices. It was fixed by replacing swipe calls with `touch.down` -> `touch.move` -> `touch.up`.
- The user confirmed the popup+video stage is temporarily stable enough to keep and asked to move focus backward to the earlier App Store download stage.
- For the App Store stage, the user clarified two separate failure modes that currently both cause endless reload loops: (1) no/weak network, where App Store may later request password/verification after connectivity returns; (2) App Store UI language/layout changes so the expected image condition no longer matches.
- A concrete App Store network-failure screen was identified from user-provided evidence: `Cannot Connect to App Store` with a `Retry` button. The user says this layout is usually fixed and tapping `Retry` makes App Store try to reconnect. Conclusion for next step: add a dedicated App Store no-network handler rather than treating this as generic UI mismatch.
- Recommendation discussed for App Store no-network detection assets: crop the popup region rather than using a full-screen screenshot, ideally saving one cropped image for the popup and one for the `Retry` button, to make matching more robust.
- User made this a standing rule: always treat `D:\OpenClaw\` as the primary working directory and default root for collaboration/deliverables on the PC side. The user says they will always keep files there and wants this remembered going forward.
- User established a fixed XXTouch on-device working layout to remember permanently across machines: script assets live under `/var/mobile/Media/1ferver/lua/scripts/`, and images live under `/var/mobile/Media/1ferver/lua/scripts/img/`. Treat this as the default on-device path unless the user explicitly says otherwise.
- App Store download step was refined further: the user created and placed these runtime image assets under `D:\OpenClaw\XXTouch_jobs\upload\img` on PC and `/var/mobile/Media/1ferver/lua/scripts/img/` on devices: `Cannot.png`, `Retry.png`, `ChuaTai.png`, `TaiXong_Eng.png`, `TaiXong_Jp.png`. These map respectively to: App Store no-network screen, Retry tap target, TikTok not-downloaded state, downloaded/open state in English, and downloaded/open state in Japanese.
- For the App Store step, the user clarified the durable detection rule: ignore text language and rely on the fixed button area instead. The TikTok App Store state button is considered position-fixed, and both not-downloaded and downloaded states should be checked around that same location. The tap point the user finalized is `(338, 645)`.
- The App Store-only test script `D:\OpenClaw\XXTouch_jobs\upload\Final_step_appstore_download_test.lua` was iteratively adjusted to: handle `Cannot Connect to App Store` by tapping `Retry`, remove overlapping countdown status updates, widen/soften image matching, avoid reloading the TikTok App Store link too aggressively, and finally anchor both detection and tapping to the fixed TikTok button area with tap point `(338, 645)`.
- XXTouch controller/API behavior learned this session: device file upload uses JSON key `filename` (not `path`) with base64 payload. This was confirmed by pushing the five new App Store image assets successfully to machine `tiktok04:184`.
- XXTouch remote screen-power endpoints were partially confirmed by direct API probing: `lock_screen` and `unlock_screen` exist and return success, while guessed paths like `screen_off`, `screen_on`, and several help/api listing paths returned HTTP 403.
- A bulk `unlock_screen` run was executed for cluster `tiktok04` machines `1-450`; result was `430/450 OK`, with the rest failing mainly by timeout or `No route to host`.
- For running existing Lua on devices, the durable rule remains: when the user says run `Final.lua` on machines, it means the already-present file `/var/mobile/Media/1ferver/lua/scripts/Final.lua` on each device, not any draft or PC-side replacement. Batch reruns were issued for `tiktok04:2-26`; machines `2-25` acknowledged OK while machine `26` repeatedly failed by timeout/no route.
