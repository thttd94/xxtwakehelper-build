- User preference: use `D:\OpenClaw` as the primary working directory for deliverables instead of the WSL workspace path. Use `D:\OpenClaw\img` for image assets.
- User operates a large XXTouch iPhone farm (~1000 iPhone 6s devices) and wants batch orchestration instead of per-device GUI actions.
- Cluster/IP mapping provided by user:
  - Tiktok04 gateway/local `192.16.0.1`; machine 1-250 => `192.16.4.1-250`; machine 251-450 => `192.16.5.1-200`.
  - Tiktok03 gateway/local `192.14.0.1`; machine 1-250 => `192.14.5.1-250`; machine 251-312 => `192.14.4.1-62`.
  - Tiktok05 gateway/local `192.15.0.1`; machine 1-250 => `192.15.4.1-250`; machine 251-312 => `192.15.5.1-62`.
- Built local tools in `D:\OpenClaw`:
  - `xxtouch_controller.py` for cluster-aware script control (`run-selected`, `stop`, `info`).
  - `xxtouch_activate_batch.py` to inspect XXTouch auth status and batch-bind via documented endpoints (`device_auth_info`, `bind_code`) if the deployment uses code-based activation.
- Important finding from XXTouch source/API: web auth page uses `/device_auth_info` and `/bind_code`, but the user’s real-world activation flow currently appears to be Havoc/Google account login, not simple code entry.
- Important safety/ops lesson: when the user restarts/reset OpenClaw gateway, background jobs often die (SIGTERM). Long-running jobs should write logs/progress and be resumable.
- XXTouch deployment progress on Tiktok04 (machines 1-208): after retries, 200 machines had full `Final.lua + img` deployed; 8 machines still problematic: 15, 30, 45, 46, 143, 167, 192, 198.
- Specific failure modes observed on remaining/problem machines:
  - some machines report `This operation requires a valid license` when trying to create `img` dir via Lua spawn;
  - some machines report `The system is currently running another script`;
  - one machine reported `Event1.png: Failed to create file`;
  - some machines were unreachable / connection refused / 403.
- Batch run status on Tiktok04: batch runner was configured to run `Final.lua` in groups of 20 machines with 10-minute pauses. Confirmed completed batches:
  - Batch 1: machines 16-36 subset, 19/20 OK (machine 16 busy running another script).
  - Batch 2: machines 37-63 subset, 20/20 OK.
- User communication preference: dislikes reading lots of text; prefer short, direct operational updates.
- Telegram bot is configured; pairing for the user's Telegram account was approved.
- Assistant identity for this OpenClaw persona: Cún 🐶; user should be addressed as Anh Thái; timezone GMT+7.
- Telegram session became too large and felt stuck; handover summary saved in `telegram-handover.md`.
- Recent Telegram topic: optimize XXTouch Lua event detection for a TikTok/AppManager flow, especially faster event scanning in the upper-left area with slight position/size variation.

- Confirmed: machine 38 belongs to Tiktok 04, so its IP maps to 192.16.4.38. User prefers not to be asked this again in follow-ups about this machine/cluster mapping.
- Important workflow clarification from user: routine work is usually done by accessing each phone/device via its HTTP web service endpoint from the provided source, not by SSH. Example given: machine 38 is accessed at http://192.16.4.83:46952/.
- Telegram troubleshooting update: session bloat caused the bot chat to feel stuck; a handover summary was written to `telegram-handover.md` for starting a fresh conversation.
- User explicitly requested a Telegram session reset. The old Telegram session entry for user 2039938948 was removed from `sessions.json`, the old transcript was archived with a timestamp suffix, and a backup copy of `sessions.json` was created before reset.
- Gateway restart was attempted immediately after the Telegram session reset.
- Telegram bot issue diagnosis: Telegram provider/channel itself was healthy, but replies appeared to stall in backend/model processing; evidence included the bot receiving messages, showing typing, then hitting typing TTL without sending a final reply.
- User saw repeated `API rate limit reached. Please try again later.` errors while Telegram was unresponsive.
- Practical troubleshooting conclusion for Telegram: connectivity was mostly fine, but the model/backend layer could hang or rate-limit, making Telegram look frozen even when the bot was online.
- Corrected operational mapping used in this Telegram session: machine 38 / Tiktok04 is confirmed as `192.16.4.38` (not the mistaken `.83` example from an older note). XXTouch web on machine 38 responded at `http://192.16.4.38:46952/` with `deviceinfo` showing iPhone 6s / iOS 15.8.6 / XXTouch 5.4.4.
- User wants work done directly where possible, especially via XXTouch web/API, with minimal back-and-forth and short practical updates.
- For TikTok event detection, current focus is replacing `/var/mobile/Media/1ferver/lua/scripts/img/Event1.png` and `Event2.png` across Tiktok04 devices using user-provided screenshots rather than relying on app-source assets; source app scan found generic coin/live assets but not a confident exact match for the real yellow coin with letter `P`.
- Batch file-push result repeated consistently on Tiktok04 machines 1-208: 200/208 devices updated successfully via `/write_file`; persistent failures were machines `1, 45, 56, 69, 121, 129, 132, 144` with timeout / connection refused / no route errors.
- Latest successful mass update used two new screenshots from Telegram, converted to `Event1.png` and `Event2.png`, written into `D:\OpenClaw\XXTouch_jobs\upload` locally and then pushed to `/var/mobile/Media/1ferver/lua/scripts/img/` on reachable devices.
- User asked to launch `Final.lua` on a specific Tiktok04 subset: `2,5,6,8,9,10,12,13,14,16,17,19,20,23,24,25,32,29,30,33,34,35,36,37,39,41,42,43,44,54,55,57,61,63,64,65,67,68,70,74,75,76,77,80,85,86,88,89,90,91,99,101,102,105,107,109,110,111,112,115,114,120,121`.
- Result of that targeted `Final.lua` launch: 60/63 OK. Failures were machine `68` and `101` (`The system is currently running another script`) and machine `121` (timeout). Recommended next recovery action is stop-and-rerun for `68` and `101`, and retry `121` separately.

- Late-session batch operations on Tiktok04 kept using XXTouch `/write_file` to distribute `Event1.png` and `Event2.png` from `D:\OpenClaw\XXTouch_jobs\upload` to `/var/mobile/Media/1ferver/lua/scripts/img/` across machines 1-208.
- Repeated mass-push result remained stable at 200/208 reachable devices. The recurring unreachable/problem machines were `1, 45, 56, 69, 121, 129, 132, 144` with timeout, no route, or connection refused.
- User asked to run `Final.lua` on a selected Tiktok04 subset; result was 60/63 OK, with machine `68` and `101` busy running another script and `121` timing out.
- User prefers direct execution with minimal chatter: when they say stop/retry or push files, just do it and report the exact OK/error counts.
