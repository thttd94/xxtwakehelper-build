- Dọn config model OpenClaw: chỉ giữ lại simpleverse/c-gpt-5.4 làm model duy nhất; bỏ default OpenAI để tránh failover 401 và làm Telegram trông như bị treo.
- Investigated Telegram "treo" after gateway update. Gateway and Telegram channel were healthy; apparent hang was due to model/runtime failures, not bot disconnect.
- Cleaned OpenClaw config to keep only provider/model `simpleverse/c-gpt-5.4`; removed OpenAI default/failover from active config so both defaults and `telegramlite` point to `simpleverse/c-gpt-5.4`.
- Confirmed via direct provider probing that Simpleverse endpoint supports `POST /chat/completions` and does not support `POST /responses` (404). Relevant provider config should stay on `api: openai-completions`.
- Ongoing issue after config cleanup: `simpleverse/c-gpt-5.4` still intermittently returns runtime/format errors (`400 status code (no body)`, `401 invalid_format`, occasional timeout) during real agent handling, which makes Telegram appear stuck.
- Separate issue observed: Control UI had `gateway token mismatch`; if dashboard/control UI acts flaky, its stored gateway token likely needs to be refreshed manually.
