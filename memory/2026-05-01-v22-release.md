# 2026-05-01 V2.2 release / corrected packaging

- User corrected my misunderstanding: merely setting VERSION/Git is not enough. "Đóng gói V2.2" must match earlier V1.1 version-backup semantics: every version has a complete package under backup storage so `rollback_version.sh <version>` restores the full version state; backups are preserved and usable online/offline.
- Created `GEN_NEW_9001/gen_backup/versions/2.2/package` containing the full V2.2 package snapshot (excluding `.git`, `__pycache__`, and outer `gen_backup`).
- Updated `GEN_NEW_9001/rollback_version.sh` to search both:
  - `/data/genrouter_backups/versions`
  - `/root/genrouter_backups/versions`
  unless `BACKUP_ROOT` is explicitly set.
- Copied updated rollback script into V2.2 package.
- Uploaded V2.2 package to live router `192.14.0.1`:
  - `/root/genrouter_backups/versions/2.2/package`
  - `/data/genrouter_backups/versions/2.2/package`
- Copied updated rollback script to `/opt/proxy-manager-v1/rollback_version.sh` and made it executable.
- Verified live router:
  - root V2.2 package VERSION: `V2.2`
  - data V2.2 package VERSION: `V2.2`
  - rollback script searches `/data` then `/root`.
- GitHub `thttd94/GEN` branch `main` updated with commit `abab2aa Package V2.2 rollback backup` after earlier V2.2 GUI commit `c488f56`.
- Note: this specific router did not currently show root/data V1.1 backup files during verification (`missing`), despite Git containing `gen_backup/versions/1.1/package`. Need not delete old backups; if user needs V1.1 restored onto this live router, seed it from Git package first.
