# 2026-03-31

- OpenClaw gateway had UI freeze symptoms around 01:38 ICT; service was restarted and recovered.
- Telegram-side work is still in progress. Durable context to resume:
  - Telegram agent `telegramlite` is the problematic path, not the main webchat agent.
  - The direct Telegram session `agent:telegramlite:telegram:direct:2039938948` is heavily bloated at roughly `123k/128k` context and is the main suspect for repeated hanging/slow behavior.
  - The related slash session `agent:telegramlite:telegram:slash:2039938948` also exists and may need reset together with the direct session.
  - Transcript file for the direct Telegram session: `/home/administrator/.openclaw/agents/telegramlite/sessions/faa2d495-5b1f-4250-aa64-f32a5f9c976d.jsonl` (about 6.9 MB, 1268 lines when checked).
  - Service restarts succeeded multiple times; gateway came back healthy each time.
  - Control UI had repeated `origin not allowed` and `token mismatch` reconnect noise during restarts, but UI eventually reconnected normally.
  - Telegram transport probe is currently OK and the bot provider starts successfully after restart.
  - A model/provider auth issue also appeared on Telegram path: embedded run logged `HTTP 401: invalid or missing api key` for `simpleproxy/gpt-5.4`; config hot reload later applied the provider api key.
  - Current config state observed later: default/main model and `telegramlite` model both point to `simpleproxy/gpt-5.4`; provider list shows only `simpleproxy`.
  - `openclaw health --json` showed Telegram probe OK but channel runtime reported `running: false` / `tokenSource: none` in one check, so runtime state should be re-verified when resuming.
  - Attempts were made to quarantine/remove the bloated Telegram session from `sessions.json`, but verification showed the entries still present afterward; this cleanup did not complete successfully yet.
  - The unfinished next action is to cleanly stop the gateway, remove/quarantine both Telegram session entries from `/home/administrator/.openclaw/agents/telegramlite/sessions/sessions.json`, quarantine their `.jsonl` transcripts, restart the gateway, then verify Telegram can answer with a fresh context.
