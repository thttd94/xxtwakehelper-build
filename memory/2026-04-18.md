# 2026-04-18

- User prefers Vietnamese responses.
- User warmly welcomed assistant back.
- User asked whether the assistant can add a provider model via API.
- User asked assistant to remember this string: `20:AB:37:08:9E:1E`.
- Verified simpleproxy endpoint and model listing work.
- Verified `gpt-5.4` chat completion works through simpleproxy.
- User provided a replacement API key for simpleproxy configuration.
# 2026-04-18

- User prefers Vietnamese responses.
- User warmly welcomed assistant back.
- User asked whether the assistant can add a provider model via API.
- User asked assistant to remember this string: `20:AB:37:08:9E:1E`.
- Verified simpleproxy endpoint and model listing work.
- Verified `gpt-5.4` chat completion works through simpleproxy.
- User provided a replacement API key for simpleproxy configuration.
- Updated OpenClaw config so custom provider `simpleproxy` declares `auth: "api-key"` and `authHeader: true` in both `C:\Users\Administrator\.openclaw\openclaw.json` and `C:\Users\Administrator\.openclaw\agents\main\agent\models.json`.
- Confirmed `session.dmScope` is `per-channel-peer`, so Telegram direct chats can use a separate session/model from main webchat.
- Identified a Telegram direct session entry `agent:main:telegram:direct:2039938948` and determined it was still using `openai-codex/gpt-5.4` before channel-specific override work.
- Determined OpenClaw session-level model forcing uses `providerOverride` and `modelOverride`; Telegram session was patched toward `simpleproxy/gpt-5.4` without changing main webchat default.
