# 2026-05-01 GUI-only fixes on 192.14.0.1

- User clarified: do not touch Apply. Work only on GUI items.
- Restored SETUP ROUTER popup in `/opt/proxy-manager-v1/static/index.html`: the JS/backend existed but modal `routerModal`/input `routerIp` were missing. Added modal copied from older working version. Backend endpoints already worked (`/api/pm/router-info`, `/api/pm/router-change-lan`).
- Then user requested: remove standalone SAVE button above CHECK ALL, and fix gear button next to pencil/session label (e.g. `TIKTOK03 - Nuôi phôi`) so it opens manager to hide/delete added configs.
- Patched `/opt/proxy-manager-v1/static/index.html` only:
  - removed `<button class="btn save-strong" onclick="saveCurrent()">SAVE</button>`
  - added missing `sessionManagerModal`/`sessionManagerList` modal
  - confirmed gear still calls `openSessionManager()` and `manageSessionBtn` exists
- Restarted `/etc/init.d/proxy-manager-v1` after HTML patches.
